# Agent 测评独立依赖集：仅需安装本文件即可运行
# `ais_bench <config> --mode agent`（harbor 及其依赖一并安装）。
harbor
mmengine-lite
numpy
tqdm>=4.64.1
tabulate
orjson
psutil
pyyaml
requests
python-dotenv
windows-curses; sys_platform == "win32"

# SWE-bench DinD API key 配置模板
#
# 复制本文件到 ~/.config/swebench-dind/api_key.env 并填入真实 key:
#   mkdir -p ~/.config/swebench-dind
#   cp api_key.env.template ~/.config/swebench-dind/api_key.env
#   $EDITOR ~/.config/swebench-dind/api_key.env
#
# 注意: 本包当前 (2026-08-12) 的 launcher.py 仍硬编码路径
# /home/zengziyu/mini_matrix/scripts/api_key.env
# 如使用本模板位置 ~/.config/swebench-dind/api_key.env,
# 需先 ln -s 或直接 cp 到 mini_matrix 路径。

export OPENAI_API_KEY="sk-REPLACE-ME"
export OPENAI_API_BASE="https://api.siliconflow.cn/v1"